/**
 * Javascript functions for the hejimedia.co website.
 * Author: Eric D'Alessandro - 07/28/2022
 */

// Ensure body has loaded before script can run.
if (document.readyState == 'loading') {
    document.addEventListener('DOMContentLoaded', ready)
}else{
    ready()
}

// Global vars
// Used for changing the color and underline of the nav item that is currently selected.
var currentPageNavSelection

// Do on page load.
function ready() {

}
